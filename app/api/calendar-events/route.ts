import {NextResponse} from 'next/server';import {errorMessage,getAuthContext} from '@/lib/supabase/auth';
export async function GET(){try{const {supabase,user}=await getAuthContext();if(!user)return NextResponse.json({message:'로그인이 필요합니다.'},{status:401});const {data,error}=await supabase.from('calendar_events').select('*').order('event_date').limit(300);if(error)throw error;return NextResponse.json({events:data})}catch(error){return NextResponse.json({message:errorMessage(error)},{status:503})}}
export async function POST(request:Request){try{const {supabase,user}=await getAuthContext();if(!user)return NextResponse.json({message:'로그인이 필요합니다.'},{status:401});const body=(await request.json()) as Record<string,unknown>;const title=typeof body.title==='string'?body.title.trim().slice(0,100):'';const event_date=typeof body.event_date==='string'&&/^\d{4}-\d{2}-\d{2}$/.test(body.event_date)?body.event_date:'';if(!title||!event_date)return NextResponse.json({message:'일정과 날짜를 입력해주세요.'},{status:400});const {data,error}=await supabase.from('calendar_events').insert({user_id:user.id,title,event_date,notes:typeof body.notes==='string'?body.notes.trim().slice(0,500)||null:null}).select().single();if(error)throw error;return NextResponse.json({event:data},{status:201})}catch(error){return NextResponse.json({message:errorMessage(error)},{status:503})}}
export async function DELETE(request:Request){try{const {supabase,user}=await getAuthContext();if(!user)return NextResponse.json({message:'로그인이 필요합니다.'},{status:401});const id=new URL(request.url).searchParams.get('id');if(!id)return NextResponse.json({message:'일정을 선택해주세요.'},{status:400});const {error}=await supabase.from('calendar_events').delete().eq('id',id);if(error)throw error;return NextResponse.json({ok:true})}catch(error){return NextResponse.json({message:errorMessage(error)},{status:503})}}
