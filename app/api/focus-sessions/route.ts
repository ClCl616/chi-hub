import { NextResponse } from 'next/server';
import { createClient } from '@/lib/supabase/server';
export async function GET(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({message:'로그인이 필요합니다.'},{status:401});const {data,error}=await supabase.from('focus_sessions').select('id,task,mode,duration_seconds,completed_at').order('completed_at',{ascending:false}).limit(10);if(error)return NextResponse.json({message:error.message},{status:500});return NextResponse.json({sessions:data})}
export async function POST(request:Request){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({message:'로그인이 필요합니다.'},{status:401});const body=await request.json();if(typeof body.durationSeconds!=='number'||!['focus','short','long'].includes(body.mode))return NextResponse.json({message:'잘못된 세션입니다.'},{status:400});const {error}=await supabase.from('focus_sessions').insert({user_id:user.id,task:String(body.task??'').slice(0,80),mode:body.mode,duration_seconds:body.durationSeconds});if(error)return NextResponse.json({message:error.message},{status:500});return NextResponse.json({ok:true},{status:201})}
