import { NextResponse } from 'next/server';
import { createClient } from '@/lib/supabase/server';
export async function POST(request:Request){try{const {email,password}=await request.json();if(typeof email!=='string'||typeof password!=='string')return NextResponse.json({message:'이메일과 비밀번호를 확인해주세요.'},{status:400});const supabase=await createClient();const {error}=await supabase.auth.signInWithPassword({email,password});if(error)return NextResponse.json({message:'이메일 또는 비밀번호가 올바르지 않습니다.'},{status:401});return NextResponse.json({ok:true})}catch{return NextResponse.json({message:'Supabase 연결이 아직 설정되지 않았습니다.'},{status:503})}}
