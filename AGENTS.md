# Codex Project Instructions

이 파일의 규칙은 이 저장소 전체에 적용한다.

## 작업 시작 시 컨텍스트 복원

- 새 작업을 시작할 때 프로젝트 루트에 `CODEX_CONTEXT.md`가 있으면 먼저 끝까지 읽고 이전 작업 상태를 파악한다.
- 이어서 실제 `git status`, 최근 커밋, 관련 코드와 설정을 확인한다. 문서만 믿고 작업하지 않는다.
- 실제 프로젝트 파일의 현재 상태를 항상 우선적인 사실로 취급한다.
- `CODEX_CONTEXT.md`와 실제 코드가 충돌하면 실제 코드를 기준으로 판단하고, 확인된 현재 상태에 맞게 문서를 수정한다.
- 문서에 `확인 필요`로 표시된 내용은 추측으로 확정하지 말고 코드, 설정 또는 사용자에게 확인한다.

## 컨텍스트 문서 유지

- 중요한 기능 구현, 버그 수정, 구조 변경, 의존성 변경, 환경/배포 설정 변경 또는 재사용해야 할 기술적 결정이 발생하면 같은 작업에서 `CODEX_CONTEXT.md`를 갱신한다.
- 완료된 작업은 `현재 진행 중인 작업`이나 `다음 작업`에 남겨두지 말고 적절한 완료 항목 또는 최근 작업 기록으로 이동한다.
- 새롭게 발견한 문제, 회귀 위험, 운영상 제약 또는 TODO는 관련 항목에 기록한다.
- 단순 문구 수정, 포맷팅, 사소한 스타일 조정까지 모두 기록해 문서를 비대하게 만들지 않는다.
- 오래된 정보를 계속 누적만 하지 말고 현재 상태에 맞게 합치거나 제거한다.
- 최근 작업 기록은 핵심 변경만 간결하게 유지하고, 상세 이력은 Git log에 맡긴다.
- API 키, 토큰, 비밀번호, 쿠키, `.env.local` 실제 값, 개인정보 등 민감한 정보는 절대로 `CODEX_CONTEXT.md`나 `AGENTS.md`에 기록하지 않는다.

## 프로젝트별 운영 규칙

- 로컬 앱은 운영 Supabase 프로젝트에 연결된다. 생성·수정·삭제 테스트는 실제 데이터에 반영된다는 전제로 최소 범위에서 수행한다.
- `.env.local`은 로컬 전용이며 커밋하지 않는다. 공개 env 키 이름은 `.env.example`에만 유지한다.
- Google OAuth 변경 시 로컬과 운영 `/auth/callback` 리다이렉트를 모두 고려한다.
- Supabase schema 변경은 새 migration으로 추가하고, 사용자 데이터 테이블과 Storage에 RLS/정책을 유지한다.
- 변경 후 로컬 또는 적절한 자동 검증을 먼저 수행한다.
- 사용자가 변경을 요청하고 검증이 끝나면 로컬 커밋 후 Windows 운영 서버에 검증된 버전을 전달한다. 배포 준비/검증은 자율적으로 수행하되 환경 값 등 누락된 필수 설정은 사용자에게 요청한다.
- 2026-09-16 사용자 결정: 운영은 집의 Windows 서버, 도메인은 chi-hub.kro.kr, Tailscale/SSH는 관리 전용이다. Sites에 새로 게시하지 않는다. GitHub `origin` push는 사용자가 명시적으로 요청한 경우에만 허용한다. 승인된 서버 전달에는 Git bundle과 SSH를 사용할 수 있다.
- 운영 앱은 loopback에서만 실행하고 Caddy가 HTTPS를 담당한다. production 빌드만 공개하며 개발 서버를 포트포워딩하지 않는다. 서버 환경 파일과 릴리스는 공개 소스나 로그에 포함하지 않는다.
- 기존 dirty worktree 변경은 사용자 소유로 간주하고 덮어쓰거나 삭제하지 않는다.

## 완료 전 확인

- 요청한 동작과 관련된 코드, 빌드 또는 최소 smoke test를 확인한다.
- `git diff --check`와 `git status`로 의도한 파일만 변경됐는지 확인한다.
- 변경이 장기 컨텍스트에 영향을 주면 `CODEX_CONTEXT.md`가 실제 코드와 일치하는지 다시 확인한다.
- 새 PC가 이 저장소만 받아도 필요한 비밀 값 자체를 제외한 작업 맥락, 실행법, 알려진 문제, 다음 단계가 복원되는 상태를 유지한다.
